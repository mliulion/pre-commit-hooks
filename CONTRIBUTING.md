# Makefile

```sh
make               # muestra una ayuda

make clean-all     # borra .venv y artefactos
make install       # crea .venv e instala todo

make pre-commit     # ejecuta los pre-commit hooks
```
